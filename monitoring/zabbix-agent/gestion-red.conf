# Custom checks for the authorized lab.
# Install scripts under /usr/local/libexec/zabbix and restart zabbix-agent.
UserParameter=gestion.folder.size[*],/usr/local/libexec/zabbix/gestion-red-dir-size.sh "$1"
UserParameter=gestion.if.status[*],/usr/local/libexec/zabbix/gestion-red-iface-status.sh "$1"
